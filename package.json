{
  "name": "smartcloud-agent-composer-source",
  "version": "1.2.4",
  "private": true,
  "workspaces": [
    "core",
    "admin",
    "tests"
  ],
  "scripts": {
    "build": "npm run build --workspace=@smart-cloud/agent-composer-core && npm run build:wp --workspace=@smart-cloud/agent-composer-admin",
    "build:wpsuite-preset": "node tools/build-wpsuite-preset-package.mjs",
    "lint": "npm run lint --workspace=@smart-cloud/agent-composer-core && npm run lint --workspace=@smart-cloud/agent-composer-admin",
    "typecheck": "npm run typecheck --workspace=@smart-cloud/agent-composer-core && npm run typecheck --workspace=@smart-cloud/agent-composer-admin",
    "test": "npm run test --workspace=@smart-cloud/agent-composer-core && npm run test --workspace=smartcloud-agent-composer-php-contract-tests"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@types/node": "^20.19.30",
    "ajv": "^8.20.0",
    "esbuild": "^0.28.1",
    "picomatch": "^4.0.5",
    "postcss-selector-parser": "^7.1.6",
    "eslint": "^9.39.0",
    "yaml": "^2.8.1",
    "tsup": "^8.5.1"
  },
  "overrides": {
    "esbuild": "$esbuild",
    "yaml": "$yaml"
  }
}
